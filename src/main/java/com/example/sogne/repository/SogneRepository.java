package com.example.sogne.repository;

import com.example.sogne.model.Sogn;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SogneRepository extends JpaRepository<Sogn, Integer> {
}
